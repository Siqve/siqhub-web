import { MainHeader } from "@components/MainHeader";
import { DeviceList } from "@/containers/DeviceList";

const Home = () => {
    return (
        <div>
            <MainHeader />
            <DeviceList/>
        </div>
    );
};

export default Home;
